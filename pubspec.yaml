name: fluttersafari
description: Application for storing challenges made on FlutterSafari.com website

version: 0.0.0+1

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.4.3
  intl: ^0.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/data.json
